[pytest]
markers =
    p0: critical protocol invariants (must pass)
    p1: should pass
    p2: nice to have
    slow: slower scenarios (multi-process / sockets)
    network: tests that bind real sockets
testpaths = tests
